# Giải quyết vấn đề

## Problem 1: Hiểu cách chơi Wordle

Thử chơi một vài word của trò chơi Wordle để nắm luật. Sau đó, chuẩn bị dữ liệu Words dictionary để làm tài liệu cho việc giải word. Các word dictionary sẽ được sử dụng với các mục đích sau:

- Làm danh sách các từ có thể đoán
- Làm danh sách các từ dùng để khởi tạo thuật toán
- Làm danh sách các từ dùng để kiểm thử thuật toán

Dữ liệu các word tiếng anh được lấy về từ open source trên GitHub. Nguồn các word tiếng anh: https://github.com/dwyl/english-words
Lí do chọn nguồn này vì đây đây là nguồn được update gần nhất tính ở thời điểm hiện tại.

Đánh giá dữ liệu

- Dữ liệu là một file JSON với cấu trúc gồm có key là word và value mặc định là 1
- Các word được viết dưới dạng chữ viết thường

## Problem 2: Giả lập API để lập trình

Giả lập API ở môi trường local để không gây ảnh hưởng tới môi trường production. Bộ API gồm có 3 endpoint

- `/api/v1/daily`: API kiểm tra đáp án đoán từ mỗi ngày
- `/api/v1/random`: API kiểm tra đáp án đoán từ ngẫu nhiên
- `/api/v1/word/{word}`: API tạo từ cần đoán và kiểm tra đáp án

Bộ API giả lập sẽ có flag {{TEST_MODE}} trong `.env` để kích hoạt chế độ kiểm thử. Ở chế độ này, API sẽ trả về thêm dữ liệu word trong phần header để kiểm tra tính đúng đắn của API

## Problem 3: Thiết lập thuật toán ban đầu

### 1. Thuật toán chi tiết

- **Khởi đầu chiến lược**: Mặc định sử dụng từ `"crane"` (hoặc từ có tổng điểm tần suất chữ cái cao nhất cho từ độ dài khác 5, hoặc từ do người dùng truyền vào).
- **Phản hồi & Lọc ứng viên (`matches_feedback`)**:
  - `correct`: Giữ lại các từ có chữ cái trùng khớp chính xác tại vị trí (slot) đó.
  - `present`: Giữ lại các từ có chứa chữ cái đó nhưng loại bỏ các từ có chữ cái tại vị trí hiện tại.
  - `absent`: Loại bỏ chữ cái nếu không xuất hiện ở vị trí khác; nếu chữ cái có xuất hiện cả ở trạng thái `present`/`correct`, khống chế số lượng ký tự chính xác.
  - Lặp lại quá trình phản hồi và lọc ứng viên

```pseudocode
current_guess = "CRANE" // Từ khởi đầu

result = HTTP_GET(https://wordle.votee.dev:8000/daily?guess={{current_guess}}) // Or another API in API Documentation

while CheckResult(result) is not correct:
  absent_slot = [1, 2] // Example word is absent

  current_guess = ChooseGuessWord(current_guess, absent_slot) // Replace absent_slot to another word

  result = HTTP_GET(https://wordle.votee.dev:8000/daily?guess={{current_guess}})

print(current_guess)
```

### 2. Request & Response mẫu

Thuật toán tự động giải câu đố Wordle đã được triển khai hoàn chỉnh tại endpoint:

- `GET /api/v1/resolve`

API tự động điều hướng theo flag cấu hình `TEST_MODE`:

- Khi `TEST_MODE=true`: Sử dụng evaluator nội bộ local để kiểm thử độc lập, nhanh chóng.
- Khi `TEST_MODE=false`: Gửi request trực tiếp đến API ngoài `https://wordle.votee.dev:8000`.

#### Request (`GET /api/v1/resolve?mode=daily&size=5&starting_word=crane`)

```bash
GET /api/v1/resolve?mode=daily&size=5&starting_word=crane
```

#### Response

```json
{
  "success": true,
  "mode": "daily",
  "target_word": "prima",
  "total_attempts": 4,
  "starting_word": "crane",
  "steps": [
    {
      "step": 1,
      "guess": "crane",
      "results": [
        { "slot": 0, "guess": "c", "result": "absent" },
        { "slot": 1, "guess": "r", "result": "correct" },
        { "slot": 2, "guess": "a", "result": "present" },
        { "slot": 3, "guess": "n", "result": "absent" },
        { "slot": 4, "guess": "e", "result": "absent" }
      ],
      "remaining_candidates_count": 96
    },
    {
      "step": 2,
      "guess": "ariot",
      "results": [
        { "slot": 0, "guess": "a", "result": "present" },
        { "slot": 1, "guess": "r", "result": "correct" },
        { "slot": 2, "guess": "i", "result": "correct" },
        { "slot": 3, "guess": "o", "result": "absent" },
        { "slot": 4, "guess": "t", "result": "absent" }
      ],
      "remaining_candidates_count": 10
    },
    {
      "step": 3,
      "guess": "drias",
      "results": [
        { "slot": 0, "guess": "d", "result": "absent" },
        { "slot": 1, "guess": "r", "result": "correct" },
        { "slot": 2, "guess": "i", "result": "correct" },
        { "slot": 3, "guess": "a", "result": "present" },
        { "slot": 4, "guess": "s", "result": "absent" }
      ],
      "remaining_candidates_count": 3
    },
    {
      "step": 4,
      "guess": "prima",
      "results": [
        { "slot": 0, "guess": "p", "result": "correct" },
        { "slot": 1, "guess": "r", "result": "correct" },
        { "slot": 2, "guess": "i", "result": "correct" },
        { "slot": 3, "guess": "m", "result": "correct" },
        { "slot": 4, "guess": "a", "result": "correct" }
      ],
      "remaining_candidates_count": 1
    }
  ],
  "message": "Puzzle solved successfully in 4 attempt(s)."
}
```

## Problem 4: Trực quan hoá quá trình đoán từ

### 1. Thiết kế giao diện

Giao diện hiển thị dựa vào [mẫu thiết kế này](https://share.google/e8P2quV3dlGd7UcF3).
Mẫu thiết kế sẽ lược bỏ các phần gồm có:

- Title/Navigation
- Keyboard

Bổ sung thêm các chế độ điều khiển

- Chọn chế độ đoán: Daily, Random, Selected
- Nút bắt đầu đoán từ
- Nút đoán lại từ
- Với chế độ Selected, sẽ có input ở trên để người dùng nhập từ cần đoán

### 2. Hành vi người dùng

- Mặc định chế độ đoán là Daily
- Người dùng được phép thay đổi chế độ đoán trước khi đã bắt đầu đoán từ. Người dùng có thể chọn 3 chế độ đoán: Daily, Random, Selected
- Nút bắt đầu đoán từ sẽ bắt đầu quá trình đoán từ. Khi nhấn nút bắt đầu, hệ thống sẽ gọi API resolve để giải word.
- Sau quá trình giải word, dựa vào kết quả trả về để hiển thị các word đã đoán cho đến khi giải thành công
- Nút đoán lại từ sẽ reset quá trình đoán từ
- Với chế độ Selected, sẽ có input ở trên để người dùng nhập từ cần đoán

## Problem 5: Đóng gói sản phẩm

- Source code được đóng gói lại vào docker để có thể mở nhanh app
- Code sẽ được up lên Github qua [link này]()
- Hướng dẫn khởi chạy app sẽ được đặt vào file README.md
